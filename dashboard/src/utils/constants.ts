export const TOKEN_NAME = "__ed";

export const SERVER_URL = process.env.REACT_APP_SERVER_URL || "http://localhost:8000";

// export const SERVER_URL = prodURL;
export const HTTP_URI = `${SERVER_URL}/api/v3`;

export const WS_URI = SERVER_URL;
